
// This file is deprecated. Authentication system has been removed.
// Keeping empty export to avoid breaking implicit imports if any remain, though none should.
import React from 'react';

export const AuthProvider = ({ children }) => <>{children}</>;
export const useAuth = () => ({});
